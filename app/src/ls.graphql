query Ls($dir: ID!){
  fileById(id: $dir) {
    __typename
    id
    name
    ...CommonPart
    ...FolderPart
  }
}

fragment CommonPart on FileInfo {
  __typename
  id
  name
  modifiedTime
  ... on File {
    size
  }
}

fragment FolderPart on Folder {
  children {
    ...CommonPart
  }
}

